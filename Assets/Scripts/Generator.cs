using UnityEngine;

public static class Generator {
    public static Texture2D GetPainting(Texture2D rgbPainting, params Color[] colors) {
        // todo
    }

    public static GameObject GetItemObject(GameObject model, params Material[] materials) {
        // todo
    }
}